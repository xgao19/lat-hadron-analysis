# Example TMDWF CS-kernel averaging input
#
# This is the plain-text batch interface for the CS-kernel averaging workflow.
# It reads already-generated CS-kernel outputs and averages the x-dependent
# results over the selected x region for each bT.

# Data settings
title_pattern l64c64a076_m140_fit_pz*
input_root /path/to/5-CSkernel
lattice_spacing_fm 0.076
gm T5
eta eta0
component real
nstates 1
normalization_mode raw

# CS-kernel averaging settings
scheme CG
extraction_type type2
kernel_label LO
# Optional source filters.
# pair_mode can be all, adjacent, or fixed_p1.
# reference_p1 fixes the shared P1 momentum when you want to select a specific
# fixed_p1 run; omit it to keep all matching P1 values.
pair_mode fixed_p1
reference_p1 5
bTrange 0 20
x_range 0.25 0.75
reference_pz_labels 5-6 6-7 7-8

# Output settings
results_dir /path/to/6-CSkernel-average

# Selection conditions used automatically by the workflow:
#   2 * x * pz > 1 GeV
#   2 * (1 - x) * pz > 1 GeV
#   bT * pz * x > 0.5
#   bT * pz * (1 - x) > 0.5
#
# If x_range is provided, the workflow uses that range directly and skips the
# automatic physical cuts above.
